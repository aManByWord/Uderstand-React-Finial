import { createContext } from "react";

export var CounterContext = createContext(null);
export var CounterDispatchContext = createContext(null);
export var TabContext = createContext(null);
export var TabDispatchContext = createContext(null);